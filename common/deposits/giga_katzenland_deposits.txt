
##### Deposits found on pc_katzenland planets #####

@blocker_high = 10 #from vanilla

d_katzenland_unsteady_ground = {
    time = 360 #relatively long time. Vanilla blockers are eg 270 for 2-district blockers.
    icon = d_deep_sinkhole

    is_for_colonizable = yes
    category = deposit_cat_blockers_natural

    can_be_cleared = {
        custom_tooltip = {
            fail_text = has_ranger_lodge
            planet = {
                NOT = { has_building = building_ranger_lodge }
            }
        }
    }

    resources = {
        category = deposit_blockers_natural
        cost = {
            energy = 1500 #relatively expensive. Vanilla blockers are eg 500-1000 for 2-district blockers
        }
    }

    planet_modifier = {
        planet_max_districts_add = -2
    }

    triggered_planet_modifier = {
        potential = {
            biologist_is_ranger_trigger = yes
        }
        planet_biologists_engineering_research_produces_add = @ranger_large_production
    }

    inline_script = deposits/environmental_architects_natural_blocker_modifier
    inline_script = deposits/environmental_architects_natural_blocker_pops

    inline_script = deposits/ai_weight_natural_blocker

    drop_weight = {
        weight = @blocker_high
        modifier = {
            factor = 0.5
            planet_size < 15
        }
        modifier = {
            factor = 0
            NOT = { is_planet_class = pc_katzenland }
        }
        modifier = {
            factor = 0
            num_free_districts = {
                type = district_city
                value < 2
            }
        }
    }

    prerequisites = { "tech_dangerous_wildlife" } #TODO
}

d_katzenland_glassflats = {
    time = 360 #relatively long time. Vanilla blockers are eg 270 for 2-district blockers.

    is_for_colonizable = yes
    category = deposit_cat_blockers_natural

    can_be_cleared = {
        custom_tooltip = {
            fail_text = has_ranger_lodge
            planet = {
                NOT = { has_building = building_ranger_lodge }
            }
        }
    }

    resources = {
        category = deposit_blockers_natural
        cost = {
            energy = 1500 #relatively expensive. Vanilla blockers are eg 500-1000 for 2-district blockers
        }
    }

    planet_modifier = {
        planet_max_districts_add = -2
    }

    triggered_planet_modifier = {
        potential = {
            biologist_is_ranger_trigger = yes
        }
        planet_biologists_energy_produces_add = @ranger_large_production
    }

    inline_script = deposits/environmental_architects_natural_blocker_modifier
    inline_script = deposits/environmental_architects_natural_blocker_pops

    inline_script = deposits/ai_weight_natural_blocker

    drop_weight = {
        weight = @blocker_high
        modifier = {
            factor = 0.5
            planet_size < 15
        }
        modifier = {
            factor = 0
            NOT = { is_planet_class = pc_katzenland }
        }
        modifier = {
            factor = 0
            num_free_districts = {
                type = district_city
                value < 2
            }
        }
    }

    prerequisites = { "tech_dangerous_wildlife" } #TODO
}

d_katzenland_sandblasting_winds = {
    time = 360 #relatively long time. Vanilla blockers are eg 270 for 2-district blockers.
    icon = d_dust_desert

    is_for_colonizable = yes
    category = deposit_cat_blockers_natural

    can_be_cleared = {
        custom_tooltip = {
            fail_text = has_ranger_lodge
            planet = {
                NOT = { has_building = building_ranger_lodge }
            }
        }
    }

    resources = {
        category = deposit_blockers_natural
        cost = {
            energy = 1500 #relatively expensive. Vanilla blockers are eg 500-1000 for 2-district blockers
        }
    }

    planet_modifier = {
        planet_max_districts_add = -2
    }

    triggered_planet_modifier = {
        potential = {
            biologist_is_ranger_trigger = yes
        }
        planet_biologists_minerals_produces_add = @ranger_large_production
    }

    inline_script = deposits/environmental_architects_natural_blocker_modifier
    inline_script = deposits/environmental_architects_natural_blocker_pops

    inline_script = deposits/ai_weight_natural_blocker

    drop_weight = {
        weight = @blocker_high
        modifier = {
            factor = 0.5
            planet_size < 15
        }
        modifier = {
            factor = 0
            NOT = { is_planet_class = pc_katzenland }
        }
        modifier = {
            factor = 0
            num_free_districts = {
                type = district_city
                value < 2
            }
        }
    }

    prerequisites = { "tech_dangerous_wildlife" } #TODO
}

d_katzenland_frequent_simooms = {
    time = 360 #relatively long time. Vanilla blockers are eg 270 for 2-district blockers.
    icon = d_forceful_winds

    is_for_colonizable = yes
    category = deposit_cat_blockers_natural

    can_be_cleared = {
        custom_tooltip = {
            fail_text = has_ranger_lodge
            planet = {
                NOT = { has_building = building_ranger_lodge }
            }
        }
    }

    resources = {
        category = deposit_blockers_natural
        cost = {
            energy = 1500 #relatively expensive. Vanilla blockers are eg 500-1000 for 2-district blockers
        }
    }

    planet_modifier = {
        planet_max_districts_add = -2
    }

    triggered_planet_modifier = {
        potential = {
            biologist_is_ranger_trigger = yes
        }
        planet_biologists_energy_produces_add = @ranger_large_production
    }

    inline_script = deposits/environmental_architects_natural_blocker_modifier
    inline_script = deposits/environmental_architects_natural_blocker_pops

    inline_script = deposits/ai_weight_natural_blocker

    drop_weight = {
        weight = @blocker_high
        modifier = {
            factor = 0.5
            planet_size < 15
        }
        modifier = {
            factor = 0
            NOT = { is_planet_class = pc_katzenland }
        }
        modifier = {
            factor = 0
            num_free_districts = {
                type = district_city
                value < 2
            }
        }
    }

    prerequisites = { "tech_dangerous_wildlife" } #TODO
}

d_katzenland_roving_dunes = {
    time = 360 #relatively long time. Vanilla blockers are eg 270 for 2-district blockers.
    icon = d_searing_desert

    is_for_colonizable = yes
    category = deposit_cat_blockers_natural

    can_be_cleared = {
        custom_tooltip = {
            fail_text = has_ranger_lodge
            planet = {
                NOT = { has_building = building_ranger_lodge }
            }
        }
    }

    resources = {
        category = deposit_blockers_natural
        cost = {
            energy = 1500 #relatively expensive. Vanilla blockers are eg 500-1000 for 2-district blockers
        }
    }

    planet_modifier = {
        planet_max_districts_add = -2
    }

    triggered_planet_modifier = {
        potential = {
            biologist_is_ranger_trigger = yes
        }
        planet_biologists_minerals_produces_add = @ranger_large_production
    }

    inline_script = deposits/environmental_architects_natural_blocker_modifier
    inline_script = deposits/environmental_architects_natural_blocker_pops

    inline_script = deposits/ai_weight_natural_blocker

    drop_weight = {
        weight = @blocker_high
        modifier = {
            factor = 0.5
            planet_size < 15
        }
        modifier = {
            factor = 0
            NOT = { is_planet_class = pc_katzenland }
        }
        modifier = {
            factor = 0
            num_free_districts = {
                type = district_city
                value < 2
            }
        }
    }

    prerequisites = { "tech_dangerous_wildlife" } #TODO
}

d_katzenland_orographic_currents = {
    is_for_colonizable = yes
    use_for_min_max_adjustments = yes

    category = deposit_cat_energy

    # use_weights_for_terraforming_swap_types = yes #TODO?
    # should_swap_deposit_on_terraforming = yes
    # terraforming_swap_types = {
    #     d_submerged_ore_veins
    #     d_volcanic_rich_mountain
    # }

    planet_modifier = {
        district_generator_max_add = 3
    }

    # potential = { #TODO alongside terraforming swaps?
    #     OR = {
    #         is_planet_class = pc_continental
    #         is_planet_class = pc_desert
    #         is_planet_class = pc_alpine
    #         is_planet_class = pc_arctic
    #         is_planet_class = pc_arid
    #         is_planet_class = pc_tundra
    #         is_planet_class = pc_gaia
    #         is_planet_class = pc_relic
    #     }
    # }

    drop_weight = { #idk if this will do anything in practice but hey
        weight = @deposit_chance_high
        modifier = {
            factor = 0
            NOT = { is_planet_class = pc_katzenland }
        }
    }
}

d_katzenland_fissile_formations = {
    icon = d_ore_rich_caverns
    is_for_colonizable = yes
    use_for_min_max_adjustments = yes

    category = deposit_cat_energy

    # use_weights_for_terraforming_swap_types = yes #TODO?
    # should_swap_deposit_on_terraforming = yes
    # terraforming_swap_types = {
    #     d_submerged_ore_veins
    #     d_volcanic_rich_mountain
    # }

    planet_modifier = {
        district_generator_max_add = 1
    }

    # potential = { #TODO alongside terraforming swaps?
    #     OR = {
    #         is_planet_class = pc_continental
    #         is_planet_class = pc_desert
    #         is_planet_class = pc_alpine
    #         is_planet_class = pc_arctic
    #         is_planet_class = pc_arid
    #         is_planet_class = pc_tundra
    #         is_planet_class = pc_gaia
    #         is_planet_class = pc_relic
    #     }
    # }

    drop_weight = { #idk if this will do anything in practice but hey
        weight = @deposit_chance_high
        modifier = {
            factor = 0
            NOT = { is_planet_class = pc_katzenland }
        }
    }
}

d_katzenland_thermoconductive_bedrock = {
    icon = d_mineral_fields
    is_for_colonizable = yes
    use_for_min_max_adjustments = yes

    category = deposit_cat_energy

    # use_weights_for_terraforming_swap_types = yes #TODO?
    # should_swap_deposit_on_terraforming = yes
    # terraforming_swap_types = {
    #     d_submerged_ore_veins
    #     d_volcanic_rich_mountain
    # }

    planet_modifier = {
        district_generator_max_add = 2
    }

    # potential = { #TODO alongside terraforming swaps?
    #     OR = {
    #         is_planet_class = pc_continental
    #         is_planet_class = pc_desert
    #         is_planet_class = pc_alpine
    #         is_planet_class = pc_arctic
    #         is_planet_class = pc_arid
    #         is_planet_class = pc_tundra
    #         is_planet_class = pc_gaia
    #         is_planet_class = pc_relic
    #     }
    # }

    drop_weight = { #idk if this will do anything in practice but hey
        weight = @deposit_chance_high
        modifier = {
            factor = 0
            NOT = { is_planet_class = pc_katzenland }
        }
    }
}

d_katzenland_metamorphosed_debris = {
    icon = d_crater
    is_for_colonizable = yes
    use_for_min_max_adjustments = yes

    category = deposit_cat_minerals

    # use_weights_for_terraforming_swap_types = yes #TODO?
    # should_swap_deposit_on_terraforming = yes
    # terraforming_swap_types = {
    #     d_submerged_ore_veins
    #     d_volcanic_rich_mountain
    # }

    planet_modifier = {
        district_mining_max_add = 3
    }

    # potential = { #TODO alongside terraforming swaps?
    #     OR = {
    #         is_planet_class = pc_continental
    #         is_planet_class = pc_desert
    #         is_planet_class = pc_alpine
    #         is_planet_class = pc_arctic
    #         is_planet_class = pc_arid
    #         is_planet_class = pc_tundra
    #         is_planet_class = pc_gaia
    #         is_planet_class = pc_relic
    #     }
    # }

    drop_weight = { #idk if this will do anything in practice but hey
        weight = @deposit_chance_high
        modifier = {
            factor = 0
            NOT = { is_planet_class = pc_katzenland }
        }
    }
}

d_katzenland_stripped_topsoil = {
    icon = d_quicksand_basin
    is_for_colonizable = yes
    use_for_min_max_adjustments = yes

    category = deposit_cat_minerals

    # use_weights_for_terraforming_swap_types = yes #TODO?
    # should_swap_deposit_on_terraforming = yes
    # terraforming_swap_types = {
    #     d_submerged_ore_veins
    #     d_volcanic_rich_mountain
    # }

    planet_modifier = {
        district_mining_max_add = 1
    }

    # potential = { #TODO alongside terraforming swaps?
    #     OR = {
    #         is_planet_class = pc_continental
    #         is_planet_class = pc_desert
    #         is_planet_class = pc_alpine
    #         is_planet_class = pc_arctic
    #         is_planet_class = pc_arid
    #         is_planet_class = pc_tundra
    #         is_planet_class = pc_gaia
    #         is_planet_class = pc_relic
    #     }
    # }

    drop_weight = { #idk if this will do anything in practice but hey
        weight = @deposit_chance_high
        modifier = {
            factor = 0
            NOT = { is_planet_class = pc_katzenland }
        }
    }
}

d_katzenland_metallic_heaps = {
    icon = d_prosperous_mesa
    is_for_colonizable = yes
    use_for_min_max_adjustments = yes

    category = deposit_cat_minerals

    # use_weights_for_terraforming_swap_types = yes #TODO?
    # should_swap_deposit_on_terraforming = yes
    # terraforming_swap_types = {
    #     d_submerged_ore_veins
    #     d_volcanic_rich_mountain
    # }

    planet_modifier = {
        district_mining_max_add = 2
    }

    # potential = { #TODO alongside terraforming swaps?
    #     OR = {
    #         is_planet_class = pc_continental
    #         is_planet_class = pc_desert
    #         is_planet_class = pc_alpine
    #         is_planet_class = pc_arctic
    #         is_planet_class = pc_arid
    #         is_planet_class = pc_tundra
    #         is_planet_class = pc_gaia
    #         is_planet_class = pc_relic
    #     }
    # }

    drop_weight = { #idk if this will do anything in practice but hey
        weight = @deposit_chance_high
        modifier = {
            factor = 0
            NOT = { is_planet_class = pc_katzenland }
        }
    }
}

d_katzenland_radiothermal_bore = {
    icon = d_fuming_bog
    is_for_colonizable = yes
    use_for_min_max_adjustments = yes

    category = deposit_cat_food

    # use_weights_for_terraforming_swap_types = yes #TODO?
    # should_swap_deposit_on_terraforming = yes
    # terraforming_swap_types = {
    #     d_submerged_ore_veins
    #     d_volcanic_rich_mountain
    # }

    planet_modifier = {
        district_farming_max_add = 3
    }

    # potential = { #TODO alongside terraforming swaps?
    #     OR = {
    #         is_planet_class = pc_continental
    #         is_planet_class = pc_desert
    #         is_planet_class = pc_alpine
    #         is_planet_class = pc_arctic
    #         is_planet_class = pc_arid
    #         is_planet_class = pc_tundra
    #         is_planet_class = pc_gaia
    #         is_planet_class = pc_relic
    #     }
    # }

    drop_weight = { #idk if this will do anything in practice but hey
        weight = @deposit_chance_high
        modifier = {
            factor = 0
            NOT = { is_planet_class = pc_katzenland }
        }
    }
}

d_katzenland_subterranean_grotto = {
    icon = d_fungal_caves
    is_for_colonizable = yes
    use_for_min_max_adjustments = yes

    category = deposit_cat_food

    # use_weights_for_terraforming_swap_types = yes #TODO?
    # should_swap_deposit_on_terraforming = yes
    # terraforming_swap_types = {
    #     d_submerged_ore_veins
    #     d_volcanic_rich_mountain
    # }

    planet_modifier = {
        district_farming_max_add = 1
    }

    # potential = { #TODO alongside terraforming swaps?
    #     OR = {
    #         is_planet_class = pc_continental
    #         is_planet_class = pc_desert
    #         is_planet_class = pc_alpine
    #         is_planet_class = pc_arctic
    #         is_planet_class = pc_arid
    #         is_planet_class = pc_tundra
    #         is_planet_class = pc_gaia
    #         is_planet_class = pc_relic
    #     }
    # }

    drop_weight = { #idk if this will do anything in practice but hey
        weight = @deposit_chance_high
        modifier = {
            factor = 0
            NOT = { is_planet_class = pc_katzenland }
        }
    }
}

d_katzenland_salt_lake = {
    is_for_colonizable = yes
    use_for_min_max_adjustments = yes

    category = deposit_cat_food

    # use_weights_for_terraforming_swap_types = yes #TODO?
    # should_swap_deposit_on_terraforming = yes
    # terraforming_swap_types = {
    #     d_submerged_ore_veins
    #     d_volcanic_rich_mountain
    # }

    planet_modifier = {
        district_farming_max_add = 2
    }

    # potential = { #TODO alongside terraforming swaps?
    #     OR = {
    #         is_planet_class = pc_continental
    #         is_planet_class = pc_desert
    #         is_planet_class = pc_alpine
    #         is_planet_class = pc_arctic
    #         is_planet_class = pc_arid
    #         is_planet_class = pc_tundra
    #         is_planet_class = pc_gaia
    #         is_planet_class = pc_relic
    #     }
    # }

    drop_weight = { #idk if this will do anything in practice but hey
        weight = @deposit_chance_high
        modifier = {
            factor = 0
            NOT = { is_planet_class = pc_katzenland }
        }
    }
}